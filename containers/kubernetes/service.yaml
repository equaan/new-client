# ================================================================
# NewClient1 — Kubernetes Service
# Generated by Opt IT Backstage — DO NOT EDIT MANUALLY
# ================================================================

apiVersion: v1
kind: Service
metadata:
  name: NewClient1-svc
  namespace: default
  labels:
    app: NewClient1
    managed-by: opt-it-backstage
spec:
  selector:
    app: NewClient1
  ports:
    - name: http
      port: 80
      targetPort: 3000
      protocol: TCP
  type: ClusterIP
