# ================================================================
# new-client — Kubernetes Service
# Generated by Opt IT Backstage — DO NOT EDIT MANUALLY
# ================================================================

apiVersion: v1
kind: Service
metadata:
  name: new-client-svc
  namespace: default
  labels:
    app: new-client
    managed-by: opt-it-backstage
spec:
  selector:
    app: new-client
  ports:
    - name: http
      port: 80
      targetPort: 3000
      protocol: TCP
  type: ClusterIP
