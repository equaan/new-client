# ================================================================
# new-client — Kubernetes Ingress
# Generated by Opt IT Backstage — DO NOT EDIT MANUALLY
# ================================================================

apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: new-client-ingress
  namespace: default
  labels:
    app: new-client
    managed-by: opt-it-backstage
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
    cert-manager.io/cluster-issuer: letsencrypt-prod
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - app.example.com
      secretName: new-client-tls
  rules:
    - host: app.example.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: new-client-svc
                port:
                  number: 80
