# ================================================================
# NewClient1 — Kubernetes Ingress
# Generated by Opt IT Backstage — DO NOT EDIT MANUALLY
# ================================================================

apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: NewClient1-ingress
  namespace: default
  labels:
    app: NewClient1
    managed-by: opt-it-backstage
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
    cert-manager.io/cluster-issuer: letsencrypt-prod
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - app.example.com
      secretName: NewClient1-tls
  rules:
    - host: app.example.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: NewClient1-svc
                port:
                  number: 80
