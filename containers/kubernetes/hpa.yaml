# ================================================================
# new-client — Horizontal Pod Autoscaler
# Generated by Opt IT Backstage — DO NOT EDIT MANUALLY
# ================================================================

apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: new-client-hpa
  namespace: default
  labels:
    app: new-client
    managed-by: opt-it-backstage
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: new-client-app
  minReplicas: 1
  maxReplicas: 3
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: 80
