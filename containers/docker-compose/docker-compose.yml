# ================================================================
# NewClient1 — Docker Compose (Local Development)
# Generated by Opt IT Backstage — DO NOT EDIT MANUALLY
#
# Usage:
#   docker compose up -d
#   docker compose logs -f app
#   docker compose down
# ================================================================

version: "3.8"

networks:
  app-network:
    driver: bridge

volumes:
  db-data:

services:

  app:
    build:
      context: ../..
      dockerfile: containers/dockerfiles/Dockerfile.nodejs
    container_name: NewClient1-app
    restart: unless-stopped
    networks:
      - app-network
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: development
      PORT: "3000"
      DATABASE_URL: "postgresql://appuser:password@db:5432/appdb"
    depends_on:
      db:
        condition: service_healthy
    labels:
      client: NewClient1
      environment: local
      managed-by: opt-it-backstage

$

$
