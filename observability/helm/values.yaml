# ================================================================
# NewClient1 — Observability Helm Values
# Generated by Opt IT Backstage — DO NOT EDIT MANUALLY
# ================================================================

global:
  labels:
    client: NewClient1
    environment: dev
    managed-by: opt-it-backstage

prometheus:
  server:
    retention: "7d"
    global:
      scrape_interval: 15s
      external_labels:
        client: NewClient1
        environment: dev
  alertmanager:
    enabled: true

grafana:
  adminPassword: "abopt123"
  persistence:
    enabled: true
    size: 5Gi
  service:
    port: 3000
  datasources:
    datasources.yaml:
      apiVersion: 1
      datasources:
        - name: Prometheus
          type: prometheus
          url: http://{{ .Release.Name }}-prometheus-server
          isDefault: true
