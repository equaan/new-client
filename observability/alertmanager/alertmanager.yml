# ================================================================
# NewClient1 — Alertmanager Configuration
# Generated by Opt IT Backstage — DO NOT EDIT MANUALLY
# ================================================================

global:
  resolve_timeout: 5m

route:
  group_by: ['alertname', 'client', 'environment']
  group_wait:      30s
  group_interval:  5m
  repeat_interval: 12h
  receiver: 'default'

  routes:
    - match:
        severity: critical
      receiver: 'critical-alerts'
      repeat_interval: 1h

receivers:

  - name: 'default'
$
    # Configure a notification channel above
    # Options: slack_configs, email_configs, pagerduty_configs
$

  - name: 'critical-alerts'
$
$

inhibit_rules:
  - source_match:
      severity: 'critical'
    target_match:
      severity: 'warning'
    equal: ['alertname', 'instance']
